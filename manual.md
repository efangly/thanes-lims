# คู่มือการใช้งาน — Feature ใหม่ (UI ตาม requirement Rev01)

สำหรับ user ใช้ตรวจการทำงาน (UAT) หลัง grill session เสร็จ 2026-08-28
เอกสารต้นทาง: [`docs/Comment_LIMS_Rev01_26082569.md`](./docs/Comment_LIMS_Rev01_26082569.md) · แผนงาน: [`task.md`](./task.md)

## การเตรียมก่อนทดสอบ

| อย่าง | ค่า |
|---|---|
| Frontend | `npm run dev` → http://localhost:3000 |
| Backend | `go run ./cmd/api` ที่ `../backend` → :8080 (ต้อง `make migrate-up` ครั้งแรก) |
| Login | `admin@thanes-lims.demo` / `Passw0rd!Demo` (qa / scientist / general ใช้รหัสเดียวกัน) |
| รีเซ็ตข้อมูลทดสอบ | `make run-seed` ที่ backend — ข้อมูลที่เพิ่มระหว่างทดสอบจะหายกลับเป็น seed |

> เครื่องสแกนบาร์โค้ด: ทุกช่องที่มีไอคอน → เป็น text input ธรรมดา รองรับทั้งเครื่องสแกน USB/Bluetooth (ยิง keystroke + Enter) และพิมพ์มือ **ยิงค้นเมื่อกด Enter เท่านั้น** หาไม่เจอจะขึ้น toast แดงและคงข้อความเดิมไว้ให้สแกน/แก้ซ้ำได้ทันที

---

## 1. ผู้ขาย (Vendor) — เมนู "ข้อมูลหลัก › ผู้ขาย (Vendor)"

**สิ่งที่เปลี่ยน:** โมดูลใหม่ทั้งหมด + `<VendorSelect>` ที่ใช้ซ้ำในฟอร์มเครื่องมือ/คลัง

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 1.1 | เปิดหน้า ผู้ขาย (Vendor) | เห็นตารางรายชื่อผู้ขาย + ช่องค้นหา (ชื่อ / ผู้ติดต่อ / เบอร์โทร) |
| 1.2 | กด "เพิ่มผู้ขาย" กรอกชื่อ + ผู้ติดต่อ + โทร + อีเมล + ที่อยู่ → บันทึก | toast "เพิ่มผู้ขายเรียบร้อย" แถวใหม่ขึ้นในตาราง จำนวนรวมเพิ่ม |
| 1.3 | กด "แก้ไข" ที่แถวใดแถวหนึ่ง แก้ข้อมูล → บันทึก | ข้อมูลอัปเดตในตาราง (ใช้ modal ตัวเดียวกับเพิ่ม) |
| 1.4 | สังเกตทั้งหน้า | **ไม่มีปุ่มลบ** — vendor ถูกอ้างด้วย FK จาก equipment/inventory/PO |
| 1.5 | ในฟอร์มเพิ่มเครื่องมือ/รายการคลัง เปิด dropdown ผู้ขาย เลือก "+ เพิ่มผู้ขายใหม่" | เปิด modal ซ้อน บันทึกแล้วเลือกผู้ขายใหม่ให้อัตโนมัติ + แสดง contact แบบอ่านอย่างเดียวใต้ dropdown |

---

## 2. ตำแหน่งจัดเก็บ — สองต้นไม้ (เมนู "ข้อมูลหลัก › ตำแหน่งจัดเก็บ")

**สิ่งที่เปลี่ยน:** เพิ่มต้นไม้ที่สองสำหรับเครื่องมือ&คลัง ใช้ view เดียวกัน (ADR-0008)

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 2.1 | เปิดหน้า มีแท็บ "ตำแหน่งตัวอย่าง / ตำแหน่งเครื่องมือ & คลัง" | เริ่มที่ต้นไม้ตัวอย่าง (cabinet → shelf → slot → sub_slot) |
| 2.2 | กดแท็บ "ตำแหน่งเครื่องมือ & คลัง" | URL เปลี่ยนเป็น `?kind=equipment_storage` · label เปลี่ยนเป็น "สร้างอาคารใหม่" / "อาคารทั้งหมด" · ลำดับชั้น building → room → zone → cabinet → shelf |
| 2.3 | drill-down เข้า node แล้ว refresh หน้า (F5) | ยังอยู่ต้นไม้เดิม ไม่เด้งกลับต้นไม้ตัวอย่าง (kind อยู่ใน URL) |
| 2.4 | สลับแท็บไปมา | เด้งกลับ root ของต้นไม้นั้นเสมอ |
| 2.5 | ดูต้นไม้เครื่องมือ | **ไม่มี**คอลัมน์/ป้าย occupancy หรือปุ่มผูก sample (แสดงเฉพาะต้นไม้ตัวอย่าง) |

---

## 3. ทะเบียนตัวอย่าง — filter + barcode + สติ๊กเกอร์ (โมดูล 01, requirement 1.1–1.2)

### 3.1 Filter bar
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 3.1.1 | ช่อง "สแกนบาร์โค้ด" พิมพ์ Barcode ID (เช่น `SMP-BC-00040`) กด Enter | กรองเหลือรายการเดียว ("พบ 1 รายการ") + เลือกให้อัตโนมัติ panel ขวาโชว์ตำแหน่งจัดเก็บ |
| 3.1.2 | พิมพ์บาร์โค้ดมั่ว กด Enter | toast แดง ช่องไม่ถูกล้าง ข้อความถูก select ไว้ให้พิมพ์ทับ |
| 3.1.3 | ช่อง "ชื่อตู้ / ตำแหน่งจัดเก็บ" พิมพ์ชื่อตู้บางส่วน | กรองด้วยชื่อ leaf (ILIKE) — ไม่ใช่ชื่อเครื่องมือทดสอบ |
| 3.1.4 | dropdown "ผู้ดูแล" เลือกคน | กรองตามผู้ดูแลปัจจุบัน |
| 3.1.5 | ล้าง filter ทั้งหมด | กลับมาแสดง list เต็ม |

### 3.2 รับตัวอย่างใหม่ (modal 2 ขั้น)
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 3.2.1 | กด "รับตัวอย่างใหม่" กรอกข้อมูล (มี **Description** + ผู้ดูแลเป็น dropdown) + จะกรอก Barcode ID เองก็ได้ → บันทึก | ไปขั้น 2 "บาร์โค้ด & สติ๊กเกอร์" |
| 3.2.2 | ขั้น 2 กด "Gen" | ได้เลขบาร์โค้ดจาก backend (`SMP-BC-xxxxx`) — **ห้าม gen เองฝั่ง frontend** กดซ้ำได้ผลเดิม (idempotent) |
| 3.2.3 | เลือก template (cap 9.5×6.4 / stem 20.5×6.5 / small 40×20 / medium 60×30) + symbology (code128 / qr) แล้วสั่งพิมพ์ | เปิด PDF สติ๊กเกอร์ **ในแท็บใหม่** ให้กด print เอง · ค่าที่เลือกถูกจำไว้ใน localStorage รอบหน้า |
| 3.2.4 | ในตารางทะเบียน ดูคอลัมน์ | มีคอลัมน์ "Barcode ID" **เพิ่ม** (ไม่แทนที่รหัส `SMP-2569-xxxxx`) + ปุ่มพิมพ์สติ๊กเกอร์ซ้ำ · Description อยู่ใน panel ขวา |

---

## 4. ย้ายตำแหน่งตัวอย่างด้วยการสแกน (โมดูล 01, requirement 1.3)

**สิ่งที่เปลี่ยน:** ปุ่มหัวตารางเปลี่ยนป้ายเป็น "ย้ายตำแหน่ง (สแกน)" — แทน animation ปลอมเดิมที่สุ่มตัวอย่าง

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 4.1 | กด "ย้ายตำแหน่ง (สแกน)" | modal ขั้น 1: ช่องสแกนบาร์โค้ดตัวอย่าง |
| 4.2 | สแกน/พิมพ์ Barcode ID ของตัวอย่าง กด Enter | โชว์ตัวอย่างที่เจอ (รหัส + ชื่อ + สถานะ) แล้วไปขั้น 2 |
| 4.3 | ขั้น 2 สแกนบาร์โค้ดตู้ | ถ้า **ไม่ใช่ leaf** → เข้า browse เฉพาะ subtree ของตู้นั้น แสดง**เฉพาะ leaf ที่ว่าง** · leaf ที่มีตัวอย่างครองอยู่แสดงแต่กดไม่ได้ + ป้าย "มีตัวอย่างครองอยู่" |
| 4.4 | เลือก/สแกน leaf ว่างปลายทาง → ยืนยัน | `PATCH /samples/{id}/location` สำเร็จ · Chain of Custody เพิ่มรายการย้าย |
| 4.5 | ตรวจว่า **ระบบไม่เลือก leaf ว่างให้อัตโนมัติ** | ต้องเป็นคนเลือกช่องเองทุกครั้ง |
| 4.6 | กด "← สแกนตัวอย่างอื่น" | กลับขั้น 1 ทำรายการถัดไปได้ (โหมดสแกนต่อเนื่อง) |

---

## 5. เครื่องมือ — field ใหม่ + หน้ารายละเอียด + เอกสาร (โมดูล 02, requirement 2.1)

### 5.1 ฟอร์มเพิ่มเครื่องมือ
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 5.1.1 | กด "เพิ่มเครื่องมือ" | ฟอร์มมี: S/N, ประเภท (Category), ผู้ผลิต (Manufacturer), รุ่น (Model), Installation Date, `<VendorSelect>`, Location picker (ต้นไม้เครื่องมือ&คลัง) |
| 5.1.2 | กรอกครบ → บันทึก | เครื่องมือใหม่ขึ้นในตาราง · TypeCode ที่ระบบ gen เป็นคนละตัวกับ Category ที่กรอก |

### 5.2 หน้ารายละเอียด `/equipment/{รหัส}` (full route — ADR-0007)
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 5.2.1 | คลิกแถวในตารางทะเบียนเครื่องมือ | เปิดหน้าเต็ม (ไม่ใช่ `?e=`) URL = `/equipment/EQ-XXX-001` |
| 5.2.2 | การ์ด "ข้อมูลทรัพย์สิน" กด "แก้ไข" แก้ field แล้วบันทึก | `PATCH /equipment/{id}` · ส่งค่าว่าง = clear (vendor/location/installation date) |
| 5.2.3 | การ์ด "เอกสารแนบ" กด "เพิ่มเอกสาร" | เปิด `upload-document` ที่ preset `type=warranty` + equipment_id · ซ่อน dropdown ประเภท/สิทธิ์ · ไฟล์ยังโผล่ในโมดูลเอกสารตามปกติ |
| 5.2.4 | การ์ด "ประวัติสอบเทียบล่าสุด" | 5 รายการล่าสุด + ลิงก์ "ดูทั้งหมด" → `/equipment/calibration-results?equipment_id=...` |

### 5.3 ตารางทะเบียนเครื่องมือ
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 5.3.1 | ดูคอลัมน์ | เพิ่ม S/N + ตำแหน่ง · **ตัด "ชั่วโมงใช้งานสะสม" ออก** (ย้ายไปหน้ารายละเอียด) |
| 5.3.2 | ช่องค้นหา พิมพ์ชื่อ หรือ S/N | ค้นได้ทั้งสองอย่าง |

---

## 6. สอบเทียบ — schedules + หน้าผลการสอบเทียบ (โมดูล 02, requirement 2.2, ADR-0006)

### 6.1 Calibration Schedules (ในหน้ารายละเอียดเครื่องมือ)
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 6.1.1 | การ์ด "ตารางสอบเทียบ" กด "เพิ่มรอบ" กรอก label + วันครบกำหนด + interval (เดือน) — ไม่ใส่ interval ก็ได้ | รอบใหม่ขึ้นในรายการ |
| 6.1.2 | รอบที่ **ไม่ตั้ง interval** | มีป้าย "ต้องกรอกวันถัดไปเองทุกครั้ง" |
| 6.1.3 | กด "ลบ" ที่รอบใดรอบหนึ่ง | ขึ้น confirm เตือนว่าเป็น **hard delete** ก่อน จึงลบจริง |

### 6.2 สถานะในตารางทะเบียนเครื่องมือ
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 6.2.1 | ดูคอลัมน์ "รอบสอบเทียบถัดไป" + วงแหวน + Tag | อ่านจาก schedule ที่ **due เร็วที่สุด** |
| 6.2.2 | เครื่องที่ยังไม่มี schedule เลย | แสดง "ยังไม่ตั้งรอบสอบเทียบ" (ไม่ fallback ไปค่า scalar เดิม) |

### 6.3 บันทึกผลสอบเทียบ (modal 2 ขั้น)
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 6.3.1 | กด "บันทึกผลสอบเทียบ" (จากหัวหน้าเครื่องมือ หรือหน้ารายละเอียด) | ขั้น 1: เลือกเครื่อง (ค้นชื่อ/SN — **S/N แสดงอ่านอย่างเดียว**) · จากหน้ารายละเอียดจะเลือกเครื่องไว้ล่วงหน้า |
| 6.3.2 | เลือก "ประเภทการสอบเทียบ" | dropdown = label ของ schedule ของเครื่องนั้น |
| 6.3.3 | กรอกค่าที่วัดได้ + ค่ายอมรับ + เลือก ผ่าน/ไม่ผ่าน → บันทึกผล | `PATCH /equipment/{id}/calibration` · frontend เติม `next_calibration_due` จาก schedule ให้เอง (**ไม่มีช่องนี้ในฟอร์ม**) |
| 6.3.4 | ขั้น 2 กด "แนบใบรับรอง" | เปิด `upload-document` preset `type=certificate` + calibration_event_id (หรือกด "ไว้ทีหลัง") |
| 6.3.5 | เครื่องที่ยังไม่มี schedule | **บล็อก**การบันทึกผล + มีลิงก์ไปเพิ่มรอบ |

### 6.4 หน้า `/equipment/calibration-results`
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 6.4.1 | เปิดจากลิงก์ "ดูทั้งหมด" หรือปุ่ม "ผลการสอบเทียบ" | ตารางผลข้ามเครื่องเรียงใหม่ → เก่า |
| 6.4.2 | search bar: คำค้น / เครื่องมือ / ผล / ตั้งแต่วันที่ / ถึงวันที่ | กรองได้ · เข้าจากลิงก์ที่มี `?equipment_id=` จะ preselect เครื่อง |
| 6.4.3 | ตรวจแถวที่เพิ่งบันทึกใน 6.3 | คอลัมน์ "ครบกำหนดถัดไป" มีค่าที่เติมมาจาก schedule อัตโนมัติ |

---

## 7. ฟอร์มรายการคลัง (โมดูล 04, requirement 3.x)

**สิ่งที่เปลี่ยน:** modal เดียวโหมดคู่ create/edit · **ตัดช่องจำนวนคงเหลือออก** (สต็อกเข้าได้ทางเดียวคือ lot)

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 7.1 | กด "เพิ่มรายการ" | ฟอร์มมี: ผู้ดูแล (dropdown user — **บังคับ**), Manufacturer (free text), `<VendorSelect>`, Location picker (ต้นไม้เครื่องมือ&คลัง) · **ไม่มีช่องจำนวน** |
| 7.2 | ไม่เลือกผู้ดูแล → กดบันทึก | ฟอร์มไม่ผ่าน (ผู้ดูแลบังคับ) |
| 7.3 | กรอกครบ → บันทึก | ถามต่อ "รับของเข้าคลังเลยไหม?" → ลิงก์ไป `/inventory/receive?item=<id>` (ไม่ยิง create + receive ต่อกันเอง) |
| 7.4 | แถวในตารางคลัง กด "แก้ไข" | เปิด modal เดิมโหมด edit (`PATCH /inventory/{id}`) |

---

## 8. รับของเข้าคลัง `/inventory/receive` (โมดูล 04, requirement 3.2)

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 8.1 | จุดเข้า 1: ปุ่ม "รับของเข้าคลัง" บนหัวหน้า inventory | เข้าหน้าเปล่า ให้ค้น/สแกนหารายการเอง |
| 8.2 | จุดเข้า 2: ปุ่ม "รับของเข้า" ในแถวตาราง | เข้าหน้าพร้อม `?item=<id>` เลือกรายการไว้ล่วงหน้า |
| 8.3 | กรอก Lot No. (**บังคับ**) + Expire Date (ไม่บังคับ) + QTY (> 0) → "รับเข้าคลัง" | `POST /inventory/{id}/receive` · toast บอกจำนวน + lot |
| 8.4 | หลังบันทึก | ฟอร์มถูกล้างพร้อมรับตัวถัดไป · list "รับเข้าในรอบนี้" ด้านล่างเพิ่มบรรทัด · list "ล็อตที่มีอยู่" ใต้ฟอร์มอัปเดต |
| 8.5 | ใส่ Lot No. ที่ซ้ำกับ lot เดิม | ระบบเตือน "lot ซ้ำ = บวกทบ lot เดิม" ก่อนกดบันทึก (ไม่ใช่ error) |
| 8.6 | ทำทีละรายการ | **ไม่มีตะกร้าหลายบรรทัด** — 1 รายการ 1 lot ต่อ request |

### 8.7 ตารางคลัง + KPI
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 8.7.1 | ดูคอลัมน์ "หมดอายุใกล้สุด" | แสดงวันหมดอายุของ lot ที่ใกล้สุด + `(n ล็อต)` · badge แดง = เลยกำหนด, เหลือง = ≤ 30 วัน |
| 8.7.2 | ดู KPI ด้านบน "ล็อตใกล้หมดอายุ" | นับจำนวน item ที่มี lot ใกล้/เลยหมดอายุ |
| 8.7.3 | หลังรับของใน 8.3 | qty คงเหลือ + วันหมดอายุใกล้สุดในตารางอัปเดตตาม |

---

## ข้อควรตรวจข้ามทุก feature

- ทำรายการโดยไม่มีสิทธิ์ (403) → ข้อความ "ไม่มีสิทธิ์ทำรายการนี้" ชัดเจน
- กด Escape ใน modal ซ้อน (เช่น quick-add ผู้ขาย) → ปิดเฉพาะ modal บนสุด ไม่ปิด modal แม่พร้อมฟอร์มที่กรอกค้าง
- Refresh หน้าที่มี query param (`?kind=`, `?s=`, `?equipment_id=`, `?item=`) → สถานะคงเดิม ไม่เด้ง
