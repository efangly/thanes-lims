# คู่มือ UAT — ตำแหน่งจัดเก็บแบบ 3 แผง (Flexible 3-Column Panels)

ฟังก์ชันใหม่ตาม **ADR-0010** · release `0.0.11` · หน้า **ข้อมูลหลัก › ตำแหน่งจัดเก็บ** (`/locations`)
เอกสารต้นทาง: [`docs/adr/0010-locations-three-panel-browser.md`](./docs/adr/0010-locations-three-panel-browser.md)

รื้อหน้า `/locations` จากเดิม (ไล่ดูทีละระดับคอลัมน์เดียว) เป็นเบราว์เซอร์ 3 แผงแบบ eLabNext:
ต้นไม้ตำแหน่ง (ซ้าย) · เนื้อหาของโหนดที่เลือก / กริดกล่อง (กลาง) · รายละเอียดโหนดหรือตัวอย่าง (ขวา)

---

## การเตรียมก่อนทดสอบ

| อย่าง | ค่า |
|---|---|
| Frontend | `npm run dev` → http://localhost:3000 |
| Backend | `make run-api` ที่ `../backend` → :8080 (ครั้งแรก `make migrate-up`) |
| Login | `admin@thanes-lims.demo` / `Passw0rd!Demo` |
| รีเซ็ตข้อมูล | `make run-seed` ที่ backend — รายการที่ย้าย/สร้างระหว่างทดสอบจะกลับเป็น seed |
| กล่องสำหรับทดสอบลาก | **Fridge-A › Shelf-1 › UITest-Box-1** (12×12, มีตัวอย่างที่ช่อง A2 / C3 / F6) |

> **หมายเหตุการลาก:** ลาก Cell ด้วยเมาส์ค้างแล้วลากช้า ๆ อย่างน้อย ~5px ระบบจึงเริ่มจับการลาก
> (กันไม่ให้คลิกธรรมดากลายเป็นการลาก)

---

## 1. โครง 3 แผง (จอกว้าง ≥ lg)

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 1.1 | เปิด `/locations` | เห็น 3 แผงเรียงเต็มความกว้าง: **ต้นไม้ตำแหน่ง · เนื้อหา · รายละเอียด** แต่ละแผงมีป้ายหัวเล็ก + ปุ่ม chevron |
| 1.2 | ลากเส้นคั่น (splitter) ระหว่างแผงไปทางซ้าย/ขวา | ความกว้างสองแผงที่ติดกันปรับตามเมาส์ · แต่ละแผงหดได้ไม่ต่ำกว่าค่าต่ำสุด (ไม่หายไปเลย) |
| 1.3 | กด chevron ที่หัวแผง "ต้นไม้ตำแหน่ง" | แผงยุบเป็นแถบแนวตั้งบาง ๆ ทางซ้าย (ชื่อแผงหมุนแนวตั้ง) · อีกสองแผงขยายเต็มพื้นที่ที่เหลือ |
| 1.4 | กดที่แถบแนวตั้งที่ยุบไว้ | แผงกางกลับคืนความกว้างเดิม |
| 1.5 | ปรับความกว้าง + ยุบแผงขวา แล้ว **refresh (F5)** | layout กลับมาเหมือนก่อน refresh (จำใน `localStorage` คีย์ `lims.locations.panels`) |
| 1.6 | เปิด `/locations` ในเบราว์เซอร์/เครื่องอื่น | เป็นค่าเริ่มต้น (1 / 1.3 / 1, ไม่ยุบ) — การจำเป็นราย browser ไม่ sync ข้ามเครื่อง |

---

## 2. จอแคบ (< lg เช่นมือถือ / ย่อหน้าต่าง)

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 2.1 | ย่อความกว้างหน้าต่างให้แคบ | เหลือ **แผงเดียว** + แถบสลับ (segmented) ด้านบน: ต้นไม้ตำแหน่ง / เนื้อหา / รายละเอียด |
| 2.2 | อยู่ที่แผง "ต้นไม้ตำแหน่ง" แล้วแตะเลือกโหนด | สลับไปแผง "เนื้อหา" อัตโนมัติ |
| 2.3 | ที่แผง "เนื้อหา" เปิดกล่องแล้วแตะ Cell ที่มีตัวอย่าง | สลับไปแผง "รายละเอียด" อัตโนมัติ |
| 2.4 | แตะแถบสลับกลับไป-มา | เนื้อหาแต่ละแผงคงอยู่ ไม่รีเซ็ต |

---

## 3. แผง 1 — ต้นไม้ตำแหน่ง (lazy tree)

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 3.1 | เปิดหน้า | เห็นเฉพาะโหนด root (ตู้/อาคาร) · โหนดยังไม่กาง |
| 3.2 | กด chevron หน้าโหนด | โหนดกาง โหลดลูก 1 ครั้ง (มี "กำลังโหลด…" แวบสั้น ๆ) · กด chevron ซ้ำ = หุบ (ไม่โหลดใหม่) |
| 3.3 | กดที่ **ชื่อ** โหนด (ไม่ใช่ chevron) | โหนดถูกเลือก (ไฮไลต์ teal) · โหนดกางเองด้วย · URL เป็น `/locations?node=<id>` · แผง 2/3 อัปเดต |
| 3.4 | กดชื่อโหนดลูกที่ลึกลงไปเรื่อย ๆ | โหนดบรรพบุรุษที่กางไว้ **ยังกางอยู่** ไม่หุบ ไม่มีการกระพริบทั้งหน้า (ไม่ remount) |
| 3.5 | ต้นไม้ตัวอย่าง: ดู leaf ที่มีตัวอย่างครองอยู่ | มี **จุดสี teal** ท้ายชื่อ |
| 3.6 | ต้นไม้ตัวอย่าง: ดูโหนดที่เป็นกล่อง | ท้ายชื่อมี badge `ใช้ไป/ทั้งหมด` (เช่น `3/144`) · เต็ม = แดง, ว่าง = เทา |
| 3.7 | คัดลอก URL `/locations?node=<id>` เปิดแท็บใหม่ / refresh | โหลดมาที่โหนดนั้น (แผง 2/3 ถูก) · ต้นไม้แสดง root หุบไว้ + แผง 2 มีแถบ path เต็ม (เช่น `Fridge-A / Shelf-1 / UITest-Box-1`) — ต้นไม้จะกางบรรพบุรุษให้เองเฉพาะตอนไล่คลิกในเซสชัน |
| 3.8 | เปิดลิงก์เก่ารูปแบบ `/locations/<id>` | redirect อัตโนมัติเป็น `/locations?node=<id>` |

---

## 4. แผง 2 — เนื้อหาของโหนดที่เลือก

### 4.1 ยังไม่เลือกโหนด / อยู่ที่ root
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 4.1.1 | เปิดหน้าโดยไม่เลือกอะไร | หัวแผงว่า "ตู้ทั้งหมด / อาคารทั้งหมด" · มีฟอร์ม **สร้าง root ใหม่** + รายการ root |
| 4.1.2 | สร้าง root ใหม่ | toast เขียว · root ใหม่โผล่ทั้งในรายการแผง 2 และในต้นไม้แผง 1 |

### 4.2 โหนดที่แบ่งย่อยได้
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 4.2.1 | เลือกตู้/ชั้น/ช่อง | หัวแผง = ชื่อโหนด + ป้ายระดับ · มีแถบ full path · ฟอร์ม "สร้างลูก (Generate)" (prefix + จำนวน) |
| 4.2.2 | Generate ลูก 5 ตัว | toast เขียว · ลูกใหม่ขึ้นในรายการแผง 2 และในต้นไม้ (ถ้าโหนดกางอยู่) |
| 4.2.3 | ที่ชั้น/ช่อง/sub-ช่อง (ต้นไม้ตัวอย่าง) | มีฟอร์ม **สร้างกล่อง (Box)** เพิ่ม — แถว/คอลัมน์ + ปุ่มลัด 96 / 81 / 100 |
| 4.2.4 | สร้างกล่อง | toast เขียว · กล่องใหม่อยู่ในรายการ (เปิดแล้วเป็นกริด) |
| 4.2.5 | กดปุ่ม × ลบโหนด → ยืนยัน | toast เขียว · โหนดหายจากทั้งรายการและต้นไม้ |
| 4.2.6 | กดปุ่ม "เปิด" ที่รายการลูก | เท่ากับเลือกโหนดนั้น (URL `?node=` เปลี่ยน, ต้นไม้เลื่อนตาม) |

### 4.3 โหนด leaf (ต้นไม้ตัวอย่าง, ไม่มีลูก)
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 4.3.1 | เลือก leaf ที่ยังว่าง | แถบล่างบอก "เป็นจุดจัดเก็บ (leaf) — ยังว่าง" + ปุ่ม **ผูก Sample** |
| 4.3.2 | กดผูก Sample เลือกตัวอย่าง | modal เดิม (assign) · เสร็จแล้ว leaf มีตัวอย่าง จุด occupancy ขึ้นในต้นไม้ |

### 4.4 โหนดกล่อง (Box) — กริด Cell
| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 4.4.1 | เปิด **UITest-Box-1** | กริด 12×12 · Cell A2 / C3 / F6 มีตัวอย่าง (โชว์ 4 หลักท้ายรหัส) · หัวกริดบอก "ใช้ไปแล้ว 3/144" |
| 4.4.2 | ดูรายการใต้กริด | ลิสต์ตำแหน่ง → ลิงก์รหัสตัวอย่าง → ชื่อ (เรียงตามตำแหน่ง) |
| 4.4.3 | กด "ขยายกริด" ใส่ค่ามากกว่าเดิม → บันทึก | toast เขียว · กริดใหญ่ขึ้น · ใส่ค่าน้อยกว่าเดิม = ปุ่มบันทึกไม่ทำงาน |

---

## 5. แผง 2 — ลากย้ายตัวอย่างในกล่อง (Drag-and-Drop) ⭐ ของใหม่หลัก

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 5.1 | คลิก Cell ที่มีตัวอย่าง **1 ครั้ง** (ไม่ลาก) | Cell ถูกเลือก (ขอบ teal) · แผง 3 แสดงรายละเอียดตัวอย่างนั้น |
| 5.2 | ลาก Cell ที่มีตัวอย่างไปวางที่ **Cell ว่าง** | ตัวอย่างย้ายไปช่องใหม่ · toast "ย้ายตำแหน่งในกล่องเรียบร้อย" · รายการใต้กริดอัปเดต · refresh (F5) แล้วยังอยู่ช่องใหม่ (บันทึกลง backend จริง) |
| 5.3 | ลาก Cell ที่มีตัวอย่างไปวางทับ **Cell ที่มีตัวอย่างอื่น** | ทั้งสองตัว **สลับช่องกัน** (1 batch) |
| 5.4 | ctrl-click (หรือ ⌘-click) / shift-click หลาย Cell ที่มีตัวอย่าง | ทุก Cell ที่เลือกมีขอบ teal · หัวกริดบอก "เลือก N ช่อง ลากเพื่อย้ายทั้งกลุ่ม" |
| 5.5 | ลาก Cell ตัวใดตัวหนึ่งในกลุ่มที่เลือก | **ทั้งกลุ่ม**เลื่อนไปด้วย offset เดียวกัน เป็น batch เดียว (atomic — ลงทั้งหมดหรือไม่ลงเลย) |
| 5.6 | ระหว่างลาก ลองเล็งตำแหน่งที่กลุ่มจะ**ล้นขอบกริด** หรือ**ทับ Cell ที่มีตัวอย่างนอกกลุ่ม** | Cell ปลายทาง preview เป็น **สีแดง** · ปล่อยเมาส์แล้ว**ไม่ย้าย** (ไม่มี toast) |
| 5.7 | ลากออกไปนอกกริดแล้วปล่อย | ไม่มีอะไรเกิดขึ้น การเลือกยังอยู่ |

> **ตรวจ:** การลากไม่เคยข้ามกล่อง — ถ้าอยากย้ายข้ามกล่องต้องใช้ flow สแกน/ผูกตำแหน่งตามปกติ

---

## 6. แผง 3 — รายละเอียด

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 6.1 | เลือกโหนด (ยังไม่คลิก Cell/ตัวอย่าง) | แสดง **สรุปโหนด**: Full path · Location Barcode · (กล่อง) กริด/ใช้ไป · (leaf ต้นไม้ตัวอย่าง) ตัวอย่างที่ครองอยู่ |
| 6.2 | ในกริดกล่อง คลิก Cell ที่มีตัวอย่าง | แผง 3 สลับเป็น **สรุปตัวอย่าง**: รหัส · Barcode ID · ชื่อ/ชนิด · ผู้ดูแล · สถานะ · ตำแหน่ง (full path + ช่อง) |
| 6.3 | กดปุ่ม "กลับ" ในแผง 3 | กลับไปสรุปโหนด |
| 6.4 | กดลิงก์ "เปิดหน้าตัวอย่างเต็ม (Chain of Custody)" | ไปหน้า `/samples?s=<id>` ของตัวอย่างนั้น |
| 6.5 | เลือกโหนดอื่น | แผง 3 กลับไปโหมดสรุปโหนดเสมอ (ล้างตัวอย่างที่ inspect ไว้) |

---

## 7. ต้นไม้เครื่องมือ & คลัง (`?kind=equipment_storage`)

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 7.1 | กดแท็บ "ตำแหน่งเครื่องมือ & คลัง" | โครง 3 แผงเหมือนกัน · ต้นไม้รีเซ็ตเป็น root ของต้นไม้เครื่องมือ (อาคาร → ห้อง → โซน → ตู้ → ชั้น) · URL `?kind=equipment_storage` |
| 7.2 | ดูต้นไม้ (แผง 1) | **ไม่มี**จุด occupancy / badge กล่อง (เฉพาะต้นไม้ตัวอย่าง) |
| 7.3 | เลือกโหนด → ดูแผง 3 | สรุปโหนด + section **"สิ่งที่จัดเก็บในตำแหน่งนี้"** = รายการ Equipment / Inventory ที่ `locationId` ตรงโหนดนั้น (แต่ละแถวลิงก์ไป detail) |
| 7.4 | โหนดที่ไม่มีอะไรจัดเก็บ | section แสดง "ยังไม่มีเครื่องมือ/สินค้าในตำแหน่งนี้" |
| 7.5 | แผง 3 ต้นไม้เครื่องมือ | **ไม่มี**แถว "ตัวอย่างที่ครองอยู่" และไม่มีการ inspect ตัวอย่าง |
| 7.6 | สลับแท็บกลับไป-มา | เด้งกลับ root ของต้นไม้นั้นเสมอ (โหนดที่เลือกของอีกต้นไม้ใช้ไม่ได้) |

---

## 8. เลขเวอร์ชันใน sidebar

| # | ขั้นตอน | ผลที่ควรได้ |
|---|---|---|
| 8.1 | ดูมุมซ้ายล่าง sidebar | `CLOUD · SYNCED · v0.0.11` — ตรงกับ git tag `0.0.11` (อ่านจาก `package.json`) |

---

## ข้อควรตรวจข้ามทุกส่วน

- สลับโหนดในต้นไม้ **ไม่กระพริบทั้งหน้า** และไม่รีเซ็ตต้นไม้ที่กางไว้ (route เดียว `?node=`)
- ทุก mutation (สร้าง/ลบ/generate/สร้างกล่อง/ย้ายในกล่อง/ขยายกริด) มี toast และสะท้อนทั้งแผง 2 และต้นไม้
- ลากในกล่องได้เฉพาะโหมดจัดการ (หน้า `/locations`) — ใน modal put-away/assign ยังเป็นคลิกเลือกช่องว่างเหมือนเดิม
- ผิดพลาดจาก backend (เช่น ลบโหนดที่มีลูก, ย้ายชนกัน 409) → toast แดง ไม่ค้าง
- ปิด backend แล้วเปิดหน้า → ต้นไม้ขึ้น "โหลดต้นไม้ตำแหน่งไม่สำเร็จ" ไม่ crash
